{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "description": "List the harness choices compatible with a provider/model.",
  "properties": {
    "model": {
      "description": "Optional model; refines the compatible harness set.",
      "type": [
        "string",
        "null"
      ]
    },
    "providerId": {
      "type": "string"
    }
  },
  "required": [
    "providerId"
  ],
  "title": "InterpreterHarnessListParams",
  "type": "object"
}