# Open Interpreter 0.0.53

## Models

- Bundle the maintained Codex 0.156.1 client and GPT-6 Astra, Sol, and Luna metadata; ChatGPT OAuth requests advertise the embedded client compatibility version.
- Refresh the bundled provider model catalog and examples; availability still depends on each provider account.

## Fixes

- Fix unsigned macOS release staging: build and verify the DMG before copying it into the release artifact set.
- Preserve the 0.0.52 mode-matched macOS helpers and voice runtime, unsigned Windows voice packaging, and Windows SDK fetch retry.
